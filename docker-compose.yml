version: '3.8'

services:
  postgres:
    container_name: mini-kanban-pg
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: mini-kanban